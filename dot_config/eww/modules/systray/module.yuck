(defwidget simple_systray [?class]
  (box :class "systray ${class}" :space-evenly false
    (systray :orientation "h"
      :prepend-new true
      :spacing 6
      :icon-size 18
    )
  )
)

(defwidget systray_module []
  (simple_systray :class "systray_box")
)
